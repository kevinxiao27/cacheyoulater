/// <reference types="canvas-confetti" />
import { TCanvasConfettiInstance, TConductorInstance, TDecorateOptionsFn, TGetTickAnimationFn, TRunAnimationParams } from "../types";
type TConductorOptions = {
    confetti: TCanvasConfettiInstance;
    getTickAnimation: TGetTickAnimationFn;
    decorateOptions: TDecorateOptionsFn;
};
declare class Conductor implements TConductorInstance {
    private interval;
    private readonly confetti;
    private readonly getTickAnimation;
    private readonly decorateOptions;
    constructor({ confetti, getTickAnimation, decorateOptions, }: TConductorOptions);
    shoot: () => void;
    run: ({ speed, delay, duration }: TRunAnimationParams) => void;
    pause: () => void;
    stop: () => void;
}
export default Conductor;
